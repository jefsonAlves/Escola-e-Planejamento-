import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.colegioemmovimento.app',
  appName: 'Colégio em Movimento',
  webDir: 'dist'
};

export default config;
